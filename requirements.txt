# Dependencias para la API ligera (nexo-lora-final/railway) en Render.
# Si Render hace el build desde la raíz del repo, usa este archivo.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
python-dotenv>=1.0.1
python-multipart>=0.0.12
httpx>=0.27.0
